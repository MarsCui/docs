# 异构卡纳管

::: info 文档信息
版本：v1.0
更新日期：2026-07-08
:::

## 子系统定位

异构卡纳管面向本地或私有化算力资源，帮助运营方管理地域、可用区、集群、节点、存储、镜像、模板、配额和监控，帮助普通用户创建实例、作业并查看用量。

### 新手理解

异构卡纳管像一座自有算力园区：运营方负责规划园区、接入机器和制定规则，普通用户在已开放的区域里申请资源、运行模型和查看状态。

## 核心术语速查

| 术语 | 说明 |
| --- | --- |
| 地域 / 可用区 | 资源按物理位置或逻辑隔离划分的层级。 |
| 集群 | 承载计算、存储和调度能力的 Kubernetes 集群。 |
| 节点 | 集群中实际运行实例或作业的服务器。 |
| 资源规格 | 用户创建实例或作业时选择的 CPU、内存、GPU/NPU 组合。 |
| 模板 | 把模型、框架、镜像、启动参数和显存规则组合成可部署方案。 |
| 配额 / 用量 | 控制可用资源上限，并记录实际消耗。 |

## 角色入口

| 角色 | 推荐入口 | 典型任务 |
| --- | --- | --- |
| 运营方 | [资源池](./operator/resource-pools/regions-zones/)、[模板](./operator/templates/inference-templates/)、[配额计量](./operator/quotas-metering/tenant-quotas/) | 接入集群、维护规格、开放模板、监控容量和调整配额。 |
| 普通用户 | [快速开始](./user/quick-start/inference-templates/)、[扩展服务](./user/extensions/images/)、[配额用量](./user/quotas-usage/quotas/) | 选择模板或镜像创建实例、查看资源状态和排查运行问题。 |

## 我该从哪里开始

| 你的目标 | 先看哪个入口 | 下一步 |
| --- | --- | --- |
| 管本地集群和资源池 | 异构卡纳管 | 地域 / 集群 / 规格 / 模板 |
| 管云账号和云上部署 | 多平台调度 | 云账号 / 资源池 / 授权 / 快速部署 |
| 发布或调用模型 | 模型及AI服务 | 模型市场 / 我的模型 / 体验中心 |

## 推荐阅读路径

1. 新手先读 [异构卡纳管快速入门](./getting-started/) 理解资源层级和角色边界。
2. 运营方按地域、集群、规格、模板、配额顺序准备资源。
3. 普通用户按模板、镜像、存储、实例、监控顺序完成模型服务创建。
4. 需要完整流程时阅读 [从零开始部署一个模型服务](./end-to-end/deploy-model-service/)。

## 使用前提

1. 已明确当前账号是运营方还是普通用户。
2. 已选择目标地域、可用区或资源池。
3. 涉及实例创建前，已确认规格、镜像、存储、配额和集群能力。
4. 涉及故障排查时，准备脱敏后的时间范围、资源编号、错误信息和日志摘要。

## 参数说明

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 角色类型 | 是 | 枚举 | 运营方 | 用于判断先进入资源治理链路还是用户资源使用链路。 |
| 资源范围 | 否 | 文本 | 示例地域 / 示例集群 | 用于定位地域、可用区、集群、节点和资源池。 |
| 作业对象 | 否 | 文本 | 示例模型实例 | 用于定位模型服务、在线 IDE、运行实例或训练任务。 |
| 配额对象 | 否 | 文本 | 示例租户 | 用于核对配额、额度、用量和计量明细。 |
| 排障线索 | 否 | 文本 | 脱敏资源编号 | 用于排查实例事件、日志、镜像、存储和调度问题。 |

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 入口选对 | 能区分运营方资源准备和普通用户资源使用入口 | 回到角色入口表重新定位 |
| 层级清楚 | 能说清地域、可用区、集群、节点和资源池关系 | 先阅读快速入门和资源池功能页 |
| 创建前可检查 | 能确认规格、镜像、存储、配额和集群能力 | 进入对应配置页逐项核对 |
| 排障可闭环 | 能根据事件、日志、监控和用量定位问题方向 | 准备脱敏线索后联系运营方继续处理 |

## 踩坑提示

- 用户侧看不到资源时，不要只刷新页面，应同时核对地域、权限、配额、模板、规格和集群状态。
- 实例排队或失败时，先统一地域、可用区、集群、规格和时间范围，再比较监控或用量数据。
- 运营侧调整资源池、模板、额度或存储组件前，应确认影响范围、维护窗口和回退方式。

## 常见问题

### 用户侧看不到资源

**问题现象：**

普通用户进入创建页或监控页后看不到目标地域、规格、镜像或集群数据。

**可能原因：**

- 运营方未开放资源可见范围。
- 地域、规格、镜像或模板没有完成关联。
- 当前账号缺少对应权限。

**处理方式：**

1. 先确认当前地域和账号角色。
2. 对照前提条件检查规格、模板、镜像和配额。
3. 联系运营方核对资源开放范围和账号权限。

### 实例创建失败

**问题现象：**

用户提交实例、作业或模型服务后，任务排队、失败或无法启动。

**可能原因：**

- 配额不足、集群容量不足或规格不可调度。
- 镜像拉取、启动命令、环境变量或存储挂载异常。
- 模板参数与模型能力不匹配。

**处理方式：**

1. 查看实例事件、日志和用户侧监控。
2. 核对镜像地址、启动命令、存储路径和资源规格。
3. 仍无法定位时，提供脱敏资源编号和失败时间给运营方。

## 后续操作

1. 运营方持续维护资源池、模板、配额和监控。
2. 普通用户创建资源后及时查看用量、日志和监控。
3. 发布前定期检查文档截图、示例命令和敏感信息脱敏情况。

## 注意事项

- 异构卡纳管入口页只用于判断阅读路径，具体创建、接入、扩容和排障步骤以对应功能页为准。
- 比较资源、配额或监控数据前，必须先统一地域、可用区、集群、租户和时间范围。
- 运营侧资源池配置会影响普通用户可见资源，调整前应确认影响范围和回退方式。
- 排障材料中不要暴露真实节点 IP、镜像仓库凭据、存储 AK/SK、租户名称或日志敏感片段。
